/*import org.scalatest._
import play.api.db.DB

import anorm._

import play.api.test.WithApplication
import play.api.test.Helpers._

import org.scalatestplus.play._


class AnormTest extends PlaySpec {
  val a = new WithApplication() {}

  /*"Anorm" should {
    "throw an error when a non-existent table is queried" in new WithApplication {
      DB.withConnection { implicit c =>
        SQL"""SELECT COUNT(*) FROM this_table_does_not_exist""".execute()
      }
    }

    "create a table" in new WithApplication {
      DB.withConnection { implicit c =>
        SQL"""CREATE TABLE test_table(
              name      VARCHAR(255) PRIMARY KEY,
              email     VARCHAR(255),
              password  VARCHAR(255)
            );
         """.execute()


      }
    }
  }*/
}

*/

